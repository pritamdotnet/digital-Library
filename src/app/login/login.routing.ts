import { Route } from '@angular/router';
import { LoginComponent } from './login.component';

export const LoginInRoutes: Route[] = [
    {
        path: '',
        component: LoginComponent
    }
];
